#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define pb push_back
#define FASTIO ios_base::sync_with_stdio(false),cin.tie(NULL),cout.tie(NULL)
#define vt vector
#define sz(x) int(x.size()) 
#define N 2e5+7

const int inf=1e9+7;
int main()
{
    FASTIO;
    ll n,m,c;
    cin>>n>>m>>c;
    vector<vector<ll>> v(n,vector<ll>(m)),d(n,vector<ll>(inf));
    for(ll i=0;i<n;i++)
    {
        for(ll j=0;j<m;j++) cin>>v[i][j];
    }
   for(ll i=0;i<n;i++)
   {
       for(ll j=0;j<m;j++)
       {
           vis.clear();
           vis.resize
       }
   }  
}